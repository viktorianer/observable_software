<div class="flex flex-col items-center">
  <h1 class="text-3xl font-bold text-center mb-6 text-gray-800"><%= @pattern.name %></h1>
  <%= image_tag @pattern.distorted_preview, alt: "Pattern Preview for #{@pattern.name}", class: "max-w-full" %>
</div>
